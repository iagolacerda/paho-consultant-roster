import styled from 'styled-components';

// Contém o Select padrão (width:100% por padrão) numa largura fixa e
// compacta o bastante pra caber na navbar/sidebar.
export const Wrap = styled.div`
  width: 130px;
  flex: 0 0 auto;
`;
